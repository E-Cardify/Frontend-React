import { Stack, Title } from "@mantine/core";
import { FC } from "react";

const Notifications: FC = () => {
  return (
    <Stack>
      <Title order={2}>Your notifications</Title>
    </Stack>
  );
};

export default Notifications;
